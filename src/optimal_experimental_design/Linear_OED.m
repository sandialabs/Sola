classdef Linear_OED < handle
    
    % We assume a Bayesian inverse problem with a mean zero Gaussian noise
    % model, a Gaussian prior, and a linear observation operator 
    
    properties

    end
     
     methods (Access = public)
         
         function this = Linear_OED()
 
         end

         
     end
     
end

